# Robot
The central repository linking all tools that run on the robot.
To initialize the submodules, use:
```
git submodule update --init --recursive
```
To pull the most recent version of submodules, use:
```
git submodule update --remote --recursive
```

